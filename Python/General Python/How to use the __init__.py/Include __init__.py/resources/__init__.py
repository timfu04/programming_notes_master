# __init__.py file makes Python treats directory containing it as a library/module/package

# __init__.py will be run first automatically once directory containing it is imported (in this case, "resources" directory is imported in "tutorial.py")

from resources.object_one import One
from resources.object_two import Two